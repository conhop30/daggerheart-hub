/**
 * Hero class module — Daggerheart Classes (named HeroClass to avoid
 * colliding with java.lang.Class). Depends on domain (validates a
 * class's two domain ids exist) and shared (the Feature type for
 * ClassFeatures).
 */
package com.daggerheart.hub.heroclass;
